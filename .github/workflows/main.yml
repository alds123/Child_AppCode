name: Android Emulator CI

on:
  push:
    branches: [ main ]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 25

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v3
        with:
          distribution: temurin
          java-version: 17

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Install Android system image
        run: |
          yes | $ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager --install "system-images;android-34;google_apis;x86_64"

      - name: Create AVD directory (manual fix)
        run: |
          mkdir -p ~/.android/avd
          echo "no" | $ANDROID_HOME/cmdline-tools/latest/bin/avdmanager create avd \
            -n API_36 \
            -k "system-images;android-34;google_apis;x86_64" \
            --device "pixel"
          sleep 5
          echo "hw.gpu.enabled=no" >> ~/.android/avd/API_36.avd/config.ini || echo "Could not write to config.ini"

      - name: Start emulator
        run: |
          nohup $ANDROID_HOME/emulator/emulator -avd API_36 -no-audio -no-window -gpu off &
          adb wait-for-device

      - name: Wait for emulator to fully boot
        timeout-minutes: 15
        run: |
          boot_completed=""
          retries=0
          until [[ "$boot_completed" == "1" || $retries -gt 30 ]]; do
            sleep 20
            boot_completed=$(adb -e shell getprop sys.boot_completed | tr -d '\r')
            echo "Boot completed: $boot_completed"
            ((retries++))
          done
          if [[ "$boot_completed" != "1" ]]; then
            echo "Emulator failed to boot in time."
            exit 1
          fi

      - name: Run tests
        run: mvn clean test
